from posting import Posting

# takes a document
# uses the HTML
# tokenizes it
# stemming
# stop words
# calculate frequencies
# calculate tf-idf thingy
    # keep track of HTML elements such as headers, bold, etc.
# returns a posting?
    # docParser.getPosting(doc)
        # this method calls all the other methods to make the elements of the posting